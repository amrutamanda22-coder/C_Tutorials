#include <stdio.h>
#include<math.h>
#include<stdlib.h>

int main(){
    float x = 45;

    //x = sqrt(x);
    //x = pow(x,3);
    //x = round(x);
    //x = ceil(x); //round up
    //x = floor(x); //round down
    //x = abs(x); //turn neg no to pos no
    //x = log(x);
    //x = sin(x);
    //x = cos(x);
    //x = tan(x);


    printf("%f",x);

    return 0;
}